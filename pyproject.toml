[project]
name = "work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SimonElyn", email = "simon.elynmure@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "argparse>=1.4.0",
    "faker>=40.39.0",
]

[project.scripts]
work = "work:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
